{"Image": "python:3.12-slim", "Cmd": ["sh", "-c", "rm -rf /mnt/cdrm-venv && python -m venv /mnt/cdrm-venv && /mnt/cdrm-venv/bin/pip install --quiet --upgrade pip && /mnt/cdrm-venv/bin/pip install --quiet --no-cache-dir -r /mnt/requirements.txt && /mnt/cdrm-venv/bin/pip install --quiet --upgrade pyplayready && /mnt/cdrm-venv/bin/python -c 'from pyplayready.exceptions import InvalidSession; from pywidevine.cdm import Cdm; import flask; print(\"venv rebuilt + imports OK\")'"], "HostConfig": {"Binds": ["/volume1/docker/containers/cdrm/app:/mnt"]}}
